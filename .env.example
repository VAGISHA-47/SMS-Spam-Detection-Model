# Example .env file
# Put this file at project root as `.env` or set MONGO_URI as an environment variable.
# For local MongoDB use:
MONGO_URI=mongodb://localhost:27017
# For MongoDB Atlas use the connection string provided by Atlas, e.g.:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
